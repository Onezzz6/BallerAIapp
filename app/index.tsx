import SortingScreen from './components/SortingScreen';

// Root component - serves as the entry point
// SortingScreen handles routing users to the appropriate screen based on auth/subscription status
export default function App() {
  return <SortingScreen />;
} 