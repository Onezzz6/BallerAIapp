# Auto-detect local IP address for Metro bundler
export RCT_METRO_PORT=8081
export RCT_METRO_HOST=$(ipconfig getifaddr en0 2>/dev/null || echo "localhost")
# export SKIP_BUNDLING=1  # Commented out to allow Metro connection